#include <criterion/criterion.h>
#include <stdio.h>

int make_addition(int a, int b)
{
    int result = a + b;
    return result;
}

int make_substraction(int a, int b)
{
    int result = a - b;
    return result;
}
